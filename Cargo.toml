[package]
name = "struktura"
version = "1.5.0"
edition = "2021"
rust-version = "1.70"
description = "Predict failure before it happens. Universal anomaly detection via DFA scaling analysis — spacecraft telemetry, bearings, text, biomedical. 85x faster than Python. no_std for embedded."
license = "MIT OR Apache-2.0"
repository = "https://github.com/koscak-labs/struktura"
documentation = "https://docs.rs/struktura"
homepage = "https://koscak-labs.github.io/struktura"
keywords = ["anomaly-detection", "dfa", "time-series", "spacecraft", "predictive-maintenance"]
categories = ["science", "algorithms", "no-std::no-alloc", "aerospace"]
readme = "README.md"

[features]
default = ["std"]
std = []
serde = ["dep:serde"]

[lib]
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
libm = "0.2"

[dev-dependencies]

[[example]]
name = "benchmark"
path = "examples/benchmark.rs"
