[package]
name = "struktura"
version = "0.3.3"
edition = "2021"
description = "Predict failure before it happens. Universal anomaly detection via DFA scaling analysis — bearings, heartbeats, spacecraft, drones, DNA. Zero dependencies."
license = "MIT OR Apache-2.0"
repository = "https://github.com/koscak-labs/struktura"
documentation = "https://docs.rs/struktura"
homepage = "https://koscak-labs.github.io/struktura"
keywords = ["anomaly-detection", "dfa", "time-series", "health-monitor", "fractal"]
categories = ["science", "algorithms"]
readme = "README.md"

[dependencies]

[dev-dependencies]

[[example]]
name = "benchmark"
path = "examples/benchmark.rs"
