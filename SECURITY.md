# Security

struktura processes numerical data only (CSV, stdin). it does not:
- make network requests (unless you use `--webhook`)
- execute code from input data
- access the filesystem beyond the file you point it at

## reporting vulnerabilities

email: phil@koscak.ai

please don't open a public issue for security vulnerabilities. I'll respond within 48 hours.
