# Claims audit — 2026-09-17

## Proposed GitHub repo description (max 350 chars)

> Is your data broken? Pipe any CSV, get told what changed. DFA-based structural anomaly detection for Rust — self-calibrating (no model training), no_std + alloc, generates C99 for flight computers. Tested on NASA SMAP/MSL, IMS bearings, Voyager; F1 0.655 on SMAP/MSL (closed-form AR baseline, not supervised).

(297 chars)

## Verification table

| # | Claim | Verdict | Evidence (file:line) | Wording change made |
|---|-------|---------|------------------------|----------------------|
| 1 | `speed_bench.rs` times Rust but prints hard-coded Python "typical" timings, not a real head-to-head; "85x faster than Python" is not measured against Python | **TRUE** | `examples/speed_bench.rs:12-37` — only calls `struktura::dfa(&data)` in a timed loop; lines 33-36 are `println!` statements with hard-coded strings ("~15-25 ms/call (typical)" etc.), no Python process is ever invoked. | README.md: speed table (lines ~125-133), the `# python — 85x faster than nolds` docker/python block, the alternatives table, and the top "85-112x faster than Python" line all reworded to "vs a published Python reference timing (not run head-to-head here)". Cargo.toml description untouched (no ratio claim there). docs/COMPARISON.md: speed column reworded, added note that `speed_bench.rs` only times the Rust side. |
| 2 | The ~6 KB "rover" embedded variant omits DFA entirely and has only three simpler detectors (prediction residuals, stuck-value, rolling-mean shift) | **TRUE** | `src/rover_flight.rs:1-15` (module doc: "No DFA leg... the other three catch 5/6 fault types"; "5,920 bytes total"); `FLeg` enum at `src/rover_flight.rs:33-37` = `Residual` (AR(1) prediction residual, line 148), `Stuck` (repeated-value, line 159-164), `LevelShift` (rolling-mean shift, line 172-189). Note: `src/rover.rs` is a *different* module — a telemetry simulator (`RoverSim`) that still uses the full `HybridMonitor` (with DFA); the 6 KB no-DFA variant is specifically `rover_flight.rs`. | README.md: both the `no_std` bullet and the use-cases table row for the rover generator now say "no DFA leg" and list the three detectors; size corrected to ~5.9KB (measured `5,920` bytes vs. the doc's rounded "6KB", kept both but clarified). docs/COMPARISON.md: added a note distinguishing the genuinely allocation-free flight variant from the core library. |
| 3 | `no_std` still uses `alloc`/`Vec` in the main library, so `no_std` ≠ allocator-free | **TRUE** | `src/lib.rs:35` `#![cfg_attr(not(feature = "std"), no_std)]`; `:37-38` `#[cfg(not(feature = "std"))] extern crate alloc;`; `:43` `use alloc::vec::Vec;`. Core DFA paths take/return `Vec<f64>` under `no_std` too: `dfa_fast_into` (`:223`), `dfa_into` (`:309`), `sanitize` (`:436`), `shuffle` (`:522`), `SlidingWindow.buffer: Vec<f64>` (`:685`). | Every "`no_std`" mention across README.md, Cargo.toml, llms.txt and docs/COMPARISON.md changed to "`no_std` + `alloc`" (or "requires a global allocator"), except the genuinely allocator-free `rover_flight.rs` variant, which is called out separately as fixed-array/no-heap. |
| 4 | "Zero training": the SMAP/MSL evaluator fits an autoregressive predictor to the train split | **TRUE** | `src/smap_eval.rs:7-8` (module doc: "ridge autoregression of order `p` fitted on the nominal train split — closed-form least squares, no gradient training, no GPU"); `ArPredictor::fit(train, p, lambda)` at `:23`, called from `src/bin/struktura.rs:3116` (`cmd_smap`, `--ar` path) and `src/bin/struktura.rs:268` (`detect_channel_tuned`). This is a genuine model fit to training data (closed-form least squares), so "zero training" is a defensible-but-misleading label — the fix keeps the true "no gradient descent / no GPU" distinction and drops the "zero training" phrase. | README.md SMAP section (lines ~203-213), USE_CASES.md line 9, Cargo.toml description, and llms.txt: "zero training" replaced with "no model training: baselines/predictor estimated from the signal itself" or, where the AR fit specifically applies, "no model training: a per-channel AR predictor is fit by closed-form ridge least squares on the train split (no gradient descent, no GPU)". |
| 5 | "0 false alarms across 200,000 clean samples" — what streams, what calibration assumptions | **TRUE (but under-scoped in the docs)** | `src/bin/struktura.rs:2371-2434` (`cmd_monitor_perf`, run via `struktura monitor-perf`): calibrates `HybridMonitor` on one 2,048-sample synthetic 6-channel stream (`synth_spacecraft(2048, 424242)`, line 2381-2383), then streams a **single** synthetic 200,000-sample stream from the same generator with a different seed (`synth_spacecraft(200_000, 434343)`, lines 2388-2389) and counts alarms (`alarms` var, lines 2394-2421). It is one continuous synthetic stream, not 200,000 independent real-world samples across varied conditions. Calibration assumptions are stated in `src/monitor.rs:49-64`: thresholds are EVT-based and rely on "the operational stream is distributed as the calibration stream (no regime change — that IS the fault case)" (`:53-54`); the doc explicitly says channels that violate this (random-walk / naturally trending instruments) are **not** covered (`:62-64`). | README.md "the receipts" bullet reworded to "0 false alarms on a single synthetic 200,000-sample 6-channel spacecraft stream (`struktura monitor-perf`; EVT-calibrated on a separate 2,048-sample calibration stream from the same generator)". PROMO_PACK.md: separated the RedBlue evolution's own zero-clean-alarm law (12 disjoint 1,400-sample seeds = 16,800 samples, `src/redblue.rs:35` `STREAM_LEN=1400`, called with `clean_seeds=12` at `src/bin/struktura.rs:2933`) from the unrelated 200K-sample `monitor-perf` figure — the promo draft had conflated the two under one "0 false alarms in 200K samples" line. |
| 6a | Prognosis is linear trend extrapolation with a stated nonlinear-degradation warning | **TRUE** | `src/prognosis.rs:1-7` (module doc: "Fits a least-squares line to the most recent `fit_window` points... and extrapolates to a failure threshold"); `:21-26` (doc comment: "When the true degradation is nonlinear (e.g. the IMS bearing's temporary 'healing' plateau...), the linear model's error exceeds the band — measured on IMS: predictions -66/+91 recordings... Treat the band as a floor on uncertainty, never a ceiling."). | README.md "prognosis: when will this cross the threshold?" line left as-is (already accurate, makes no nonlinear-prediction claim). No "Predict failure before it happens" phrase was found anywhere in the 6 target files, so no replacement was needed for that specific wording. |
| 6b | Does the F1 0.655 SMAP/MSL scoring count a detection anywhere inside a labeled interval as a hit (point-adjust style), or require detection before the interval starts? | **Point-adjust style (any overlap counts)** | `src/bin/struktura.rs:3126-3140` (`cmd_smap`, `--ar` path): for each predicted interval `(ps, pe)` and each labeled sequence `(s, e)`, the hit test is a plain interval-overlap check — `if ps <= e && pe >= s { hit[si] = true; }` (line 3132) — with no requirement that `ps` precede `s`. A detection landing anywhere inside, or even overlapping the edge of, a labeled anomaly window counts as a true positive. | README.md and USE_CASES.md F1 mentions now note "a detection counts as a hit anywhere inside the labeled anomaly window (point-adjust style), not only before the window starts." |

## Files changed

- `README.md`
- `Cargo.toml`
- `llms.txt`
- `USE_CASES.md`
- `PROMO_PACK.md`
- `docs/COMPARISON.md`
- `docs/CLAIMS-AUDIT-2026-09-17.md` (this file, new)

Not touched: the IMS "~2 hours" line, the Voyager/heliopause lines, the `compare`/`z` lines (per instructions — fixed by other hands earlier today), and anything under `src/`, `tests/`, or `examples/`.

## Addendum, same day (from the detector rebuild session)

| Claim | Status | Evidence |
|---|---|---|
| Old `when` changepoint counts and "% confidence" (before commit 166c030) | INVALID, removed everywhere | old detector reported 14 changepoints on shuffled white noise |
| Rebuilt `when` on stationary controls | verified 0 changes | shuffled, AR(0.7), AR(0.95), 1/f noise, 123K samples each |
| Rebuilt `when` positive control | verified | white\|walk\|white lands at exactly 8192 and 16384 |
| `guard` "(97%)" labels (before commit 2a821c2) | INVALID, removed | calibrated on synthetic sine filler, printed 97% regardless of alarm size; now observed/threshold ratio |
| `compare`/`check` "% confidence" (before commit 270d65b) | INVALID, removed | same shuffle null; now a subsampling z. CRITICAL/WARNING are fixed α thresholds, not significance |
| Voyager 1 2022 AACS "detected" | INVALID, reworded | during-vs-pre p=0.52; full files α 1.409 / 1.160 / 1.226; demo slices z=1.5 |
| Voyager heliopause "catches what ankane misses" | INCONCLUSIVE, reworded | α 1.137 → 1.056, z=0.6 on the bundled slices |
| IMS "~105 hours" | INVALID, now ~2 h | 10-min sampling, alarm at recording 970 of 984 |
| "0 false alarms on 200K samples" for `guard`/rover after 2026-09-17 | NOT RE-VALIDATED | monitor-perf number predates the guard scoring change; re-run before quoting |
| SMAP/MSL "F1 0.788" | NOT REPRODUCIBLE, replaced by 0.655 | `smap --ar 0 --dfa` gives 0.655 on 38c850a, 3724137 and 2a821c2; bare `smap` 0.161–0.167; `--ar 0` 0.605; `--ar 25` 0.577. Matrix: docs/evidence/smap-f1-2026-09-17.md |
