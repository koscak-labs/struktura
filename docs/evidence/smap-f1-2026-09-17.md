# SMAP/MSL F1: reproduction matrix, 2026-09-17

Data: data/smap_msl (JPL telemanom labels, 80 channels, 105 labeled sequences).
Scoring: a labeled sequence counts as detected if any alarm lands inside it (point-adjust);
alarms outside every labeled sequence are false positives. Same data and scorer for every row.

| code | `struktura smap` | `smap --ar 0` | `smap --ar 25` | `smap --ar 0 --dfa` |
|---|---|---|---|---|
| 38c850a (v1.7.0, first commit to claim F1 0.788) | 0.167 | 0.605 | not run | 0.655 |
| 3724137 (last pushed master, README said 0.788) | 0.167 | 0.605 (P 97.9%, R 43.8%) | 0.577 | 0.655 (P 80.6%, R 55.2%) |
| 2a821c2 (current branch head) | 0.161 (P 9.9%, R 43.8%) | 0.605 | not run | 0.655 |

Conclusion: F1 0.788 was not reproduced from any of these builds or documented flags
(`--ar`, `--dfa`, defaults for `--zmin 2.5`, `--prune 0.13`, `--buffer 50`). Its origin is
unknown. The number the docs may quote is **F1 0.655 with `struktura smap --ar 0 --dfa`**
(precision 0.806, recall 0.552), with the bare command's 0.161 stated alongside.
JPL telemanom LSTM on the same data, as printed by the tool: P 87.5%, R 80.0%.

Builds: each commit checked out into its own worktree with a standalone `[workspace]`
table appended (a stray C:\Users\philp\Cargo.toml otherwise captures the crate), built with
`cargo build --release --bin struktura`, run from the repo root so `data/smap_msl/` resolves.
